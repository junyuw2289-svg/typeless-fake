# Typeless Voice 打字工具完整功能规格

> 本文档基于对 Typeless Voice 的全面调研，详细描述了所有功能特性、UI 设计和技术实现细节。

---

## 一、核心功能清单

### 1.1 语音转文字工作流程

**基本工作流程：**
1. **激活方式**：按住快捷键（默认 Fn 键）
2. **录音**：保持按键录制语音（最长 6 分钟）
3. **停止**：松开快捷键
4. **处理**：AI 实时处理语音（1-2 秒延迟）
5. **输出**：自动将润色后的文本插入当前光标位置

**三种操作模式：**
- **Dictate（听写模式）**：自然说话，自动转换为润色文本
- **Speak to Edit（语音编辑）**：选中文本后用语音修改
- **Speak to Ask（语音提问）**：对只读内容进行总结、翻译等操作

### 1.2 智能文本处理

- ✅ **自动去除填充词**："um"、"uh"、"you know" 等口头禅
- ✅ **自动去除重复**：检测并删除不必要的重复词语
- ✅ **自我纠正识别**：说话中途改口时只保留最终意图
- ✅ **自动格式化**：列表、步骤、要点自动组织为结构化文本
- ✅ **自动标点**：智能添加标点符号和大写字母
- ✅ **特殊格式识别**：电话号码、邮箱等自动格式化

### 1.3 上下文感知

根据当前应用自动调整语气：
- Gmail/Outlook → 正式专业的邮件语气
- Slack/WhatsApp → 轻松对话式语气
- VS Code/Cursor → 技术性语言，识别代码语法
- 文档工具 → 结构化写作风格

### 1.4 多语言支持

- 🌍 支持 **100+ 语言**
- 🔄 **自动语言检测**：无需手动切换
- 🔀 **语言混合**：同一段话中无缝切换语言
- 🌐 **即时翻译**：说话同时翻译为目标语言

---

## 二、完整页面设计规格

### 2.1 登录页面（Login）

**窗口尺寸**：440px 宽

**设计元素：**

```
┌─ macOS Window Chrome (52px) ────────┐
│ ● ● ●  Typeless                    │
├─────────────────────────────────────┤
│                                     │
│          [Logo: T in square]        │
│            Typeless                 │
│        Speak, don't type            │
│                                     │
│  Email                              │
│  [_____________________________]    │
│                                     │
│  Password                           │
│  [_____________________________] 👁  │
│                Forgot password?     │
│                                     │
│  [      Sign In (Orange)       ]    │
│                                     │
│       or continue with              │
│                                     │
│  [  G Google  ]  [  Apple  ]       │
│                                     │
│  Don't have an account? Create one  │
│                                     │
└─────────────────────────────────────┘
```

**样式参数：**
- Logo：48x48px，深色背景，12px 圆角，轻微旋转
- 品牌名：28px Instrument Serif
- 输入框：46px 高，10px 圆角，1.5px 边框
- 主按钮：48px 高，橙色 #d97757
- 社交按钮：白色背景，品牌图标

### 2.2 欢迎引导页面（Welcome）

**窗口尺寸**：560px 宽

**核心元素：**

```
┌─────────────────────────────────────┐
│                                     │
│       [波形动画: 18个竖条]            │
│                                     │
│       Welcome to Typeless           │
│                                     │
│  Your voice is about to become...   │
│                                     │
│         ━  ○  ○  ○  ○              │
│                                     │
│      [    Get Started    ]          │
│                                     │
└─────────────────────────────────────┘
```

**波形动画参数：**
- 18 个竖条，6px 宽，3px 圆角
- 颜色：橙色 + 浅灰色交替
- 高度范围：20px-90px
- 动画：独立上下缩放，1.5s 循环

### 2.3 权限请求页面（Permissions）

**设计元素：**

```
┌─────────────────────────────────────┐
│                                     │
│          [麦克风图标 + 脉冲环]         │
│                                     │
│        Grant Permissions            │
│  Typeless needs a few permissions   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │ 🎤  Microphone Access    ○  │   │
│  │     Required for voice...   │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │ ⚙️  Accessibility        ○  │   │
│  │     Allows text insertion... │   │
│  └─────────────────────────────┘   │
│                                     │
│  [   Grant Permissions   ]          │
│                                     │
│         Set up later                │
│                                     │
└─────────────────────────────────────┘
```

**权限卡片样式：**
- 图标容器：42x42px，渐变背景
- 圆角：14px
- 边框：1.5px
- 未授权复选框：20px 空心圆

### 2.4 主界面布局（Dashboard）

**布局结构：**

```
┌──────────┬──────────────────────────────┐
│   Logo   │                              │
│ Typeless │                              │
│   [Pro]  │   Recent Dictations          │
│          │   Your latest voice...       │
│          │                              │
│  ● Home  │  [Export] [+ New Recording]  │
│  ○ Hist. │                              │
│  ○ Integ │  ┌──────────────────────┐   │
│  ○ Analy │  │ Meeting Notes        │   │
│  ○ Setti │  │ 2 hrs ago · 3m 42s   │   │
│          │  │ [Completed]          │   │
│          │  │ Discussed Q1...      │   │
│          │  └──────────────────────┘   │
│          │                              │
│          │  ┌──────────────────────┐   │
│          │  │ Email Draft          │   │
│  [Spacer]│  │ Yesterday · 1m 23s   │   │
│          │  │ [Completed]          │   │
│          │  └──────────────────────┘   │
│  👤 JD   │                              │
│ John Doe │                              │
│ Pro Plan │                              │
└──────────┴──────────────────────────────┘
```

**侧边栏样式：**
- 宽度：240px
- 背景：#141413（深色）
- 导航项高度：44px
- 激活状态：白色/10% 背景
- 图标：18px

### 2.5 录音状态浮动窗口

**状态 1：录音中（Recording）**
```
┌─────────────────────┐
│  ● Recording... 00:12│
└─────────────────────┘
```
- 背景：rgba(20, 20, 19, 0.85)
- 红色圆点：8px，闪烁动画
- 计时器：IBM Plex Mono

**状态 2：转录中（Transcribing）**
```
┌─────────────────────┐
│  ⟳ Transcribing...  │
└─────────────────────┘
```
- 旋转加载器：16px，橙色

**状态 3：完成（Completed）**
```
┌─────────────────────┐
│    ✓ Done ✓         │
└─────────────────────┘
```
- 背景：rgba(120, 140, 93, 0.9)
- 绿色勾选图标

**状态 4：监听中（Listening Pill）**
```
┌──────────────────────┐
│ ♪♪♪ Listening...    │
└──────────────────────┘
```
- 形状：药丸形（100px 圆角）
- 背景：深色 #141413
- 音符动画：5个竖条，3px 宽

### 2.6 应用集成页面（Integrations）

**卡片网格（3列）：**

```
┌───────────┐ ┌───────────┐ ┌───────────┐
│ #  Slack  │ │ VS VS Code│ │ ✉️  Gmail │
│   Active  │ │   Active  │ │  Paused   │
│           │ │           │ │           │
│ Casual... │ │ Tech...   │ │ Pro...    │
│           │ │           │ │           │
│ 142 dict. │ │ 89 dict.  │ │ 67 dict.  │
└───────────┘ └───────────┘ └───────────┘
```

**卡片样式：**
- 尺寸：370px 宽
- 圆角：20px
- 边框：1.5px
- 图标：40x40px，品牌色背景
- 状态徽章：12px 圆角，带圆点

---

## 三、设计系统规格

### 3.1 颜色系统

```css
/* 主色调 */
--orange: #d97757;
--orange-hover: #c4684a;
--orange-light: #f5e6df;

/* 中性色 */
--dark: #141413;
--light: #faf9f5;
--mid-gray: #b0aea5;
--light-gray: #e8e6dc;

/* 辅助色 */
--blue: #6a9bcc;
--green: #788c5d;
--red: #e05252;

/* 应用品牌色 */
--slack: #4A154B;
--vscode: #007ACC;
--chatgpt: #10A37F;
--notion: #191919;
```

### 3.2 字体系统

```css
/* 字体家族 */
--font-display: 'Instrument Serif';
--font-ui: 'DM Sans';
--font-mono: 'IBM Plex Mono';

/* 字体大小 */
--text-xs: 10px;   /* Pro 徽章 */
--text-sm: 11px;   /* 状态徽章 */
--text-base: 12px; /* 元数据 */
--text-md: 13px;   /* 标准 UI */
--text-lg: 14px;   /* 正文 */
--text-xl: 15px;   /* 按钮、卡片标题 */
--text-2xl: 16px;  /* Logo */
--text-3xl: 20px;  /* 小标题 */
--text-4xl: 24px;  /* 页面标题 */
--text-5xl: 28px;  /* 品牌名 */
--text-6xl: 30px;  /* 欢迎标题 */
```

### 3.3 间距系统

```css
--space-1: 4px;
--space-2: 7px;    /* 标签到输入框 */
--space-3: 8px;
--space-4: 12px;   /* 卡片间距、按钮间距 */
--space-5: 16px;   /* 卡片内元素 */
--space-6: 18px;   /* 表单组间距、卡片内边距 */
--space-7: 20px;   /* 卡片网格间距 */
--space-8: 24px;   /* 内容区间距 */
--space-9: 32px;   /* 页面边距、侧边栏 gap */
--space-10: 40px;  /* 页面内边距 */
--space-11: 48px;  /* 欢迎页内边距 */
```

### 3.4 圆角系统

```css
--radius-sm: 10px;   /* 输入框、按钮 */
--radius-md: 14px;   /* 卡片 */
--radius-lg: 20px;   /* 大卡片 */
--radius-xl: 24px;   /* 模态框 */
--radius-pill: 100px; /* 药丸形状 */
```

### 3.5 阴影系统

```css
--shadow-input: 0 1px 3px rgba(0,0,0,0.04);
--shadow-btn: 0 2px 8px rgba(217,119,87,0.25);
--shadow-card: 0 4px 16px rgba(20,20,19,0.06);
--shadow-window: 0 25px 60px rgba(0,0,0,0.35), 0 8px 20px rgba(0,0,0,0.2);
```

---

## 四、关键组件规格

### 4.1 主按钮

```css
.btn-primary {
  height: 48px;
  background: var(--orange);
  color: #ffffff;
  border-radius: 10px;
  font-size: 15px;
  font-weight: 600;
  padding: 0 20px;
  box-shadow: var(--shadow-btn);
  transition: all 0.2s ease;
}

.btn-primary:hover {
  background: var(--orange-hover);
  transform: translateY(-1px);
  box-shadow: 0 4px 16px rgba(217,119,87,0.35);
}
```

### 4.2 输入框

```css
.form-input {
  height: 46px;
  border: 1.5px solid var(--light-gray);
  border-radius: 10px;
  padding: 0 16px;
  font-size: 14px;
  font-family: 'DM Sans';
  background: #ffffff;
  box-shadow: var(--shadow-input);
}

.form-input:focus {
  border-color: var(--orange);
  box-shadow: 0 0 0 3px var(--orange-light);
}
```

### 4.3 卡片

```css
.card {
  background: #ffffff;
  border: 1.5px solid var(--light-gray);
  border-radius: 14px;
  padding: 18px;
  transition: all 0.2s;
}

.card:hover {
  border-color: var(--orange);
}
```

### 4.4 状态徽章

```css
.badge {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 6px 12px;
  border-radius: 16px;
  font-size: 11px;
  font-weight: 500;
}

.badge--success {
  background: rgba(120, 140, 93, 0.15);
  color: var(--green);
}

.badge--processing {
  background: rgba(217, 119, 87, 0.15);
  color: var(--orange);
}
```

### 4.5 导航项

```css
.nav-item {
  height: 44px;
  padding: 0 12px;
  border-radius: 8px;
  display: flex;
  align-items: center;
  gap: 12px;
  font-size: 14px;
  transition: background 0.2s;
}

.nav-item--active {
  background: rgba(255, 255, 255, 0.1);
  color: #ffffff;
  font-weight: 500;
}

.nav-item--inactive {
  color: rgba(255, 255, 255, 0.5);
  font-weight: 400;
}
```

---

## 五、动画规格

### 5.1 波形动画

```css
@keyframes wave {
  0%, 100% { transform: scaleY(1); }
  50% { transform: scaleY(0.4); }
}

.wave-bar {
  width: 6px;
  border-radius: 3px;
  background: var(--orange);
  animation: wave 1.5s ease-in-out infinite;
}

/* 每个竖条不同延迟 */
.wave-bar:nth-child(1) { animation-delay: 0s; }
.wave-bar:nth-child(2) { animation-delay: 0.1s; }
.wave-bar:nth-child(3) { animation-delay: 0.2s; }
/* ... */
```

### 5.2 脉冲动画（录音红点）

```css
@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.3; }
}

.recording-dot {
  width: 8px;
  height: 8px;
  background: var(--red);
  border-radius: 50%;
  animation: pulse 1.5s ease-in-out infinite;
}
```

### 5.3 音符跳动

```css
@keyframes note-jump {
  0%, 100% { height: 8px; }
  50% { height: 16px; }
}

.note-bar {
  width: 3px;
  background: var(--orange);
  border-radius: 1.5px;
  animation: note-jump 1s ease-in-out infinite;
}

.note-bar:nth-child(1) { animation-delay: 0s; }
.note-bar:nth-child(2) { animation-delay: 0.15s; }
.note-bar:nth-child(3) { animation-delay: 0.3s; }
.note-bar:nth-child(4) { animation-delay: 0.45s; }
.note-bar:nth-child(5) { animation-delay: 0.6s; }
```

### 5.4 脉冲环动画

```css
@keyframes pulse-ring {
  0% {
    transform: scale(1);
    opacity: 0.3;
  }
  100% {
    transform: scale(1.6);
    opacity: 0;
  }
}

.pulse-ring {
  border: 2px dashed var(--orange);
  border-radius: 50%;
  animation: pulse-ring 2s ease-out infinite;
}
```

---

## 六、桌面端特性

### 6.1 macOS 窗口框架

```css
.window {
  background: var(--light);
  border-radius: 12px;
  box-shadow: var(--shadow-window);
  overflow: hidden;
}

.window-chrome {
  height: 52px;
  background: linear-gradient(180deg, #f2f1ec 0%, #eeede8 100%);
  border-bottom: 1px solid var(--light-gray);
  -webkit-app-region: drag;
}

.traffic-lights {
  display: flex;
  gap: 8px;
}

.traffic-light {
  width: 12px;
  height: 12px;
  border-radius: 50%;
  box-shadow: inset 0 -1px 1px rgba(0,0,0,0.1);
}

.traffic-light--close { background: #ff5f57; }
.traffic-light--minimize { background: #febc2e; }
.traffic-light--maximize { background: #28c840; }
```

### 6.2 全局快捷键

**默认设置：**
- 主快捷键：`Fn`（Function 键）
- 备选：`Right Command`、`Right Alt`、`F2`

**工作机制：**
- 按住：开始录音
- 松开：停止录音并处理

### 6.3 权限要求

**macOS：**
- 麦克风权限：`NSMicrophoneUsageDescription`
- 辅助功能权限：`NSAccessibilityUsageDescription`

---

## 七、技术实现参考

### 7.1 前端技术栈

- React 18
- TypeScript
- Tailwind CSS
- Zustand（状态管理）

### 7.2 Electron 架构

**主进程模块：**
- `ShortcutManager` - 全局快捷键
- `AudioService` - 麦克风录音
- `TranscriptionService` - Whisper API
- `TextInjector` - 剪贴板 + 粘贴模拟

### 7.3 音频处理

- WebM/Opus 格式录制
- 转换为 WAV/MP3（Whisper 支持）
- OpenAI Whisper API（$0.006/分钟）

---

## 八、快速参考

### 8.1 页面清单

1. ✅ 登录页面（440px）
2. ✅ 注册页面
3. ✅ 欢迎页面（560px，波形动画）
4. ✅ 权限请求页面
5. ✅ 快捷键设置页面
6. ✅ 语言选择页面
7. ✅ 完成页面
8. ✅ 主界面（侧边栏 + 内容）
9. ✅ 历史详情页
10. ✅ 应用集成页
11. ✅ 统计分析页
12. ✅ 错误状态页
13. ✅ 更新通知
14. ✅ 帮助支持页

### 8.2 核心 UI 元素

- ✅ 波形动画（18 个竖条）
- ✅ 音符跳动（5 个竖条）
- ✅ 录音红点（脉冲）
- ✅ 脉冲环（权限图标）
- ✅ 状态浮动窗口
- ✅ macOS 交通灯
- ✅ 深色侧边栏
- ✅ 应用集成卡片

---

**文档版本**：1.0
**最后更新**：2026-02-20
**数据来源**：Typeless 官网、App Store、产品评测、本地设计文件分析
